{
  "name": "hris-backend",
  "version": "1.0.0",
  "description": "HRIS Backend API using Serverless Framework",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "serverless offline",
    "deploy": "serverless deploy",
    "deploy:dev": "serverless deploy --stage dev",
    "deploy:prod": "serverless deploy --stage prod"
  },
  "keywords": [
    "serverless",
    "aws",
    "lambda",
    "hris",
    "api"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "serverless": "^3.0.0",
    "serverless-offline": "^12.0.0"
  },
  "dependencies": {
    "aws-sdk": "^2.1000.0"
  }
}
